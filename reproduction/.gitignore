# offline W&B run dirs (noisy/large; CSV is the source of truth)
outputs/wandb/
outputs/wandb_cache/
outputs/wandb_home/
# handoff artifacts (large .npy imputed connectomes; regenerated, consumed locally by downstream)
outputs/artifacts/
# SLURM + run logs + sentinels (transient)
slurm-*.out
*_output.txt
DONE_*.sentinel
ERROR_*.sentinel
# keep: splits/*.json, outputs/*.csv, configs/*
